# ============================================
# DeepTutor CLI — Full Dependencies
# ============================================
# Install: pip install -r requirements/cli.txt && pip install -e .
# Everything needed to run the CLI with all tools (RAG, code execution, etc.)

# --- Config & templating ---
python-dotenv>=1.2.2
PyYAML>=6.0.3
jinja2>=3.1.6

# --- LLM ---
openai>=2.32.0
tiktoken>=0.12.0

# --- LLM provider SDKs ---
anthropic>=0.96.0
dashscope>=1.25.17
perplexityai>=0.32.1
oauth-cli-kit>=0.1.3; python_version >= "3.11"

# --- HTTP clients ---
aiohttp>=3.13.5
httpx>=0.28.1
requests>=2.33.1

# --- Async helpers ---
nest_asyncio>=1.6.0
tenacity>=9.1.4

# --- Data models & storage ---
pydantic>=2.13.3
pydantic-settings>=2.14.0
aiosqlite>=0.22.1

# --- RAG (LlamaIndex) ---
llama-index>=0.14.21
PyMuPDF>=1.27.2.2
numpy>=2.4.4,<3.0.0
arxiv>=3.0.0

# --- Web search ---
ddgs>=9.14.1

# --- CLI framework ---
typer>=0.24.1
rich>=15.0.0
prompt_toolkit>=3.0.52
